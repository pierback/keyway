import Foundation

public enum SpotifyEndpoints {
    public static let authorizeURL = URL(string: "https://accounts.spotify.com/authorize")!
    public static let tokenURL = URL(string: "https://accounts.spotify.com/api/token")!
}
